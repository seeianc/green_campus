export default function AppTest() {
  return (
    <div style={{ padding: "20px", background: "#0d1117", color: "#e6edf3", fontFamily: "monospace" }}>
      <h1>✅ React is rendering!</h1>
      <p>If you see this message, React is working correctly.</p>
      <p>Now we can debug why the real components aren't showing.</p>
    </div>
  );
}
